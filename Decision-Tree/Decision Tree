
import dt_lib
import random

def main():
    random.seed(1)
    mush = dt_lib.load_data("mushroom.txt")
    train, test = dt_lib.get_training_testing(mush)
    for index in range(1,22):
        print index, dt_lib.information_gain(mush, index)
    print make_tree(mush, range(1,22), 3)
main() 

