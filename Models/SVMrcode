library(e1071)
df = read.table("part.txt")
names(df) = c("Adid","Position","Depth","Impression","Click")
df["ctr"] = df["Click"]/df["Impression"]

train=df
svm.fit=svm(ctr~.,data=train,kernel="polynomial",degree=1,cost=1)

test=??
pred=predict(svm.fit,test)
